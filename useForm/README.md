# useForm Hook

> 14-04-2021


>Ejemplo de uso
```
    const inicialForm = {
        name: '',
        age: 0,
        email, ''
    }
    const [ formValues, handleInputChange, reset ] = useForm( inicialForm );
```
